import styled from 'styled-components';

export const StyledApp = styled.div`
  display: flex;
  min-height: 100vh;
  font-family: sans-serif;
  font-family: sans-serif;

  > main {
    width: 100%;
  }

  h2 {
    font-weight: 500
  }

`;
